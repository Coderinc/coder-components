import Button from './components/Button';
import DarkThemeProvider from './elements/DarkThemeProvider';  

// const lightTheme = {};
// Object.keys(lightThemeBase).forEach(k => {
//   lightTheme[k] = baseColors[lightThemeBase[k]] || lightThemeBase[k];
// });


module.exports = {
  DarkThemeProvider,
  Button,
};

